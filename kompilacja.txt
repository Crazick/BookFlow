Wykonywać z poziomu BookFlow a nie żadnego podkatalogu

kompilacja
mvn compile

uruchamianie
// === Server ===
mvn -q exec:java -Pserver
// === Client ===
mvn -q exec:java -Pclient

-q jest po to by na początku nie wyświetlały się komunikaty od maven